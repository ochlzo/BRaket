# Icon Rules

- Reuse icons from `marketing-icons.tsx` and primitives from `icon-base.tsx`.
- Keep icon sizing and stroke behavior consistent with existing icon components.
- Use theme-driven color classes on usage sites (`theme/semantic.ts`, `theme/tailwind.ts`) instead of hardcoded colors in icon definitions.
