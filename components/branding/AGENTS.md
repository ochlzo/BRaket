# Branding Component Rules

- Reuse `BrandMark` for BRaket logo/wordmark patterns.
- Keep brand visuals aligned with theme tokens (`--brand-*`, tone vars) and `theme/semantic.ts`.
- Use reusable typography classes (for example `typo-brand`) for brand text.
- Do not hardcode new brand colors in JSX.
