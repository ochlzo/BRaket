# Browse Route UI Rules

- Reuse shared UI blocks (`PageShell`, `SearchField`, `FilterChip`, `TalentCard`) before adding new local components.
- Use semantic/tone classes from `theme/semantic.ts` and `theme/tailwind.ts`.
- Use `typo-*` utility classes for headings, labels, and body text.
- Avoid hardcoded colors and duplicate layout markup.
