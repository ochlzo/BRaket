# Services Route UI Rules

- Reuse shared components (`PageShell`, `ServiceCard`, `CtaBanner`) before adding route-local UI components.
- Use palette/tone/semantic utilities from `theme/*`; avoid one-off color values.
- Use typography utilities (`typo-*`) from `app/globals.css` instead of repeated inline text classes.
- Keep styles and structure aligned with existing shared design system patterns.
